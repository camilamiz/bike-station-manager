<h1>Bike #<%= @bike.id%></h1>
<%= link_to "Bikes", '/bikes'%>
<div>
    <% presenter = BikePresenter.new(@bike)%>
    <p>
        Bairro: <%= presenter.current_station.neighborhood %> |
        Status: <%= presenter.current_status %>
    </p>
    <%= render 'edit' %>
</div>